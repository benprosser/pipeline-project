version: 2

sources:
  - name: project
    database: raw
    tables:
      - name: death_causes
        -columns:
          - name: country_terrain
            tests:
              - unique
              - not_null